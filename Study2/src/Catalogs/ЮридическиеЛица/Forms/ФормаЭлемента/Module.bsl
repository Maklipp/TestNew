
&НаКлиенте
Процедура ПриОткрытии(Отказ)

	Если (НЕ Параметры.Ключ.Пустая()) Тогда
		ПроверитьНаличиеБанковскихСчетов();
	КонецЕсли;

КонецПроцедуры

&НаСервере 
Процедура ПроверитьНаличиеБанковскихСчетов()
	
	СчетаВыборка = Справочники.БанковскиеСчета.Выбрать(, Объект.Ссылка);
	Если (Не СчетаВыборка.Следующий()) Тогда
		Сообщить("У данного юр. лица нет ни одного банковского счета");
	КонецЕсли;
	
КонецПроцедуры
